import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    text: {
      primary: "#FFFFFF",
    },
  },
  typography: {
    fontFamily: "Inter",
    color: "white",
  },
});
export default theme;
